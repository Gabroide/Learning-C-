int ex01()
{// Three good choices for a variable name
	int life = 0;
	bool isBusy = false;
	float distance = 12345.56F;

	// Three bad choices for a variable name
	int a = 0; // The name doesn't let to know about what the variable is for
	bool _isBusy = false; // Only some special variable should start with an underline _
	float Distance = 12345.56F; // Variable shouldn't start with an Uppercase

	return 0;
}